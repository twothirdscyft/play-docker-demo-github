machine:
  timezone:
    America/New_York
  java:
    version: openjdk7
  services:
    - redis
    
dependencies:
  pre:
    - wget -q https://dl.bintray.com/sbt/debian/sbt-0.13.5.deb
    - sudo dpkg -i sbt-0.13.5.deb

test:
  override:
    - sbt test-all
